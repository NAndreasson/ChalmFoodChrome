<div class="menu">
  <h2 class="restaurant-title">{{title}}</h2>
  <dl>
    {{#each dishes}}
      <dt class="dish-title">{{title}}</dt>
      <dd class="dish-desc">{{desc}}</dd>
    {{/each}}
  </dl>
</div>
